{
  "name": "the-inevitability-report",
  "scripts": {
    "clean": "rm -rf _site",
    "start": "npm run clean && eleventy --serve",
    "build": "npm run clean && eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}